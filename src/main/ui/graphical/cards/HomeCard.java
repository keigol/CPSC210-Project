package ui.graphical.cards;

import javax.swing.*;
import java.awt.*;

// Displays the home page
public class HomeCard extends JPanel {
    private final String name = "Home";

    // MODIFIES: this
    // EFFECTS: instantiates the home card
    public HomeCard() {
        setBackground(Color.BLUE); // tests visibility
    }
}
